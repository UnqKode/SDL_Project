# Project dependencies (install with: pip install -r requirement.txt)
fastapi>=0.121.2
uvicorn[standard]
numpy>=2.3.4
matplotlib>=3.10.7
requests>=2.32.5
scikit-learn>=1.7.2
seaborn>=0.13.2
# Install CPU-only PyTorch wheel (recommended if you don't have CUDA).
# If you need CUDA, replace the following line with the appropriate
# install command from https://pytorch.org
torch --index-url https://download.pytorch.org/whl/cpu
tqdm>=4.67.1
pydantic>=2.0
# Testing/runtime helpers
pytest>=7.0

